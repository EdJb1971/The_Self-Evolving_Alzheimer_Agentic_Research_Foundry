import { Routes, Route } from 'react-router-dom';import QuerySubmission from './components/QuerySubmission';import TaskStatusDashboard from './components/TaskStatusDashboard';import AuditTrailViewer from './components/AuditTrailViewer';import Navbar from './components/Navbar';function App() {  return (    <div className="min-h-screen bg-gray-100">      <Navbar />      <div className="container mt-8">        <Routes>          <Route path="/" element={<QuerySubmission />} />          <Route path="/query-submission" element={<QuerySubmission />} />          <Route path="/task-status" element={<TaskStatusDashboard />} />          <Route path="/audit-trail" element={<AuditTrailViewer />} />        </Routes>      </div>    </div>  );}export default App;